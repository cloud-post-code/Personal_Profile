{
  "features": [
    {
      "id": "theme-visual-picker",
      "title": "Visual theme picker: color swatches + pickers, in-font font menus, corner preview boxes",
      "status": "done",
      "notes": "Colors as swatch boxes open by default with color picker + hex entry (Colors above Corners); font menus render each option in its own white-text font (expanded curated Google Fonts library); corners as 3 preview boxes in different colors. app/admin/ThemePicker.tsx. Live."
    },
    {
      "id": "profile-experience-list",
      "title": "Experience list on Profile tab (add-one-at-a-time, animated boxes)",
      "status": "done",
      "notes": "ExperienceEditor: role/company/dates/description entries added one at a time, each box animates in like a chat message. Stored on Profile.experience (JSON) and fed into the chatbot system prompt. Live."
    },
    {
      "id": "retrieval-engine-v1",
      "title": "Retrieval engine v1: chunk/entity/edge tables, hybrid retrieval + one-hop graph expansion, persona-core prompt",
      "status": "done",
      "notes": "Sources are chunked+embedded+entity-extracted inline at ingest (lib/retrieval/*); chat builds persona-core prompt + retrieved context for the visitor's question (buildSystemPrompt(query)). Embeddings: VOYAGE_API_KEY/OPENAI_API_KEY if set, else local hashed n-grams; hybrid BM25+cosine with EntityEdge one-hop expansion. Backfill: npx tsx scripts/reindex.ts. Proof: docs/features/retrieval-engine-v1/proof.ts (green); gate PASS; verified live in browser."
    },
    {
      "id": "github-profile-import",
      "title": "Import all projects from a GitHub profile link",
      "status": "done",
      "notes": "Projects tab 'Import from GitHub': paste a profile URL, fetches public non-fork repos via GitHub API (lib/github.ts), creates a Project per repo (name, description->blurb, html_url->githubUrl, homepage->liveUrl), most-starred first, capped at 12, skips already-imported (deduped by githubUrl). Verified live against a real profile. Live."
    }
  ]
}
